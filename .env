# Gọi thẳng backend (URL đầy đủ). Không dùng proxy localhost — bỏ BACKEND_URL.
# Backend cần bật CORS cho origin http://localhost:8080 (hoặc domain deploy).
VITE_API_BASE_URL=https://e68e-34-21-131-11.ngrok-free.app/
# Theo Swagger: multipart bắt buộc field tên `file`
VITE_RAG_UPLOAD_FIELD=file
